export default function AuditLog() {
  return (
    <div data-testid="page-audit">
      <h1 className="text-2xl font-bold mb-4" data-testid="audit-title">Audit Log</h1>
      <p data-testid="audit-subtitle">Immutable audit trail of all system actions.</p>
    </div>
  )
}
