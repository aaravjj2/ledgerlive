[project]
name = "ledgerlive-api"
version = "0.1.0"
description = "LedgerLive Finance Ops Close Agent — Backend API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = ["ignore::DeprecationWarning"]
